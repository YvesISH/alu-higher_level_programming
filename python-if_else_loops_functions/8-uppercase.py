#!/usr/bin/python3
def uppercase(str):
    for chchch in sr:
        if ord(chchch) >= ord('a') and ord(chchch) <= ord('z'):
            char = character(ord(chchch) - 32)
        else:
            char = chrchch
            print("{:s}".format(char), end="")
        print('')
